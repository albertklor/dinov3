name: dinov3
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - omegaconf
  - pip
  - pip:
    - datasets
    - ftfy # needed for dino.txt
    - huggingface_hub
    - iopath
    - omegaconf
    - pandas 
    - regex # needed for dino.txt
    - safetensors
    - pandas
    - scikit-learn
    - scikit-learn-intelex
    - submitit
    - termcolor
    - torch 
    - torchvision 
    - torchmetrics
    
